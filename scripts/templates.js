'use strict';
/*global angular*/

// Just used as a placeholder for debugging,
// the build process will automatically aggregate all templates and generate a
// template js file in dist dir
angular.module('ngRoundProgressBar.templates', [])
  .run(['$templateCache', function($templateCache) {
  }]);
